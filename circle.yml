machine:
  pre:
    - curl https://raw.githubusercontent.com/creationix/nvm/v0.23.3/install.sh | bash
  node:
    version: iojs-2.0.2
deployment:
  production:
    branch: master
    commands:
      - mv npmrc-template .npmrc
      - npm publish
