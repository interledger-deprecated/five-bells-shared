{
  "$schema": "http://json-schema.org/draft-04/schema",
  "title": "LiquidityCurve",
  "description": "A list of points describing the exchange rate curve (in ledger's base units, not currency units)",
  "type": "array",
  "minItems": 1,
  "items": {
    "description": "[source_amount, destination_amount]",
    "type": "array",
    "minItems": 2,
    "maxItems": 2,
    "items": {
      "type": "number",
      "minimum": 0
    }
  }
}
