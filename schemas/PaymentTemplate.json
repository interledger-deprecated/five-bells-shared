{
  "$schema": "http://json-schema.org/draft-04/schema",
  "title": "PaymentTemplate",
  "description": "A template for a cross-ledger transfer that can have some payment fields missing",
  "type": "object",
  "properties": {
    "id": {
      "description": "Unique ID",
      "$ref": "Iri.json"
    },
    "source_transfers": {
      "description": "Transfers that credit the trader",
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "TransferTemplate.json" }
    },
    "destination_transfers": {
      "description": "Transfers that debit the trader",
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "TransferTemplate.json" }
    },
    "source_fee_transfers": {
      "description": "Fees paid to the payment system for holding funds or executing the transfer",
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "TransferTemplate.json" }
    },
    "destination_fee_transfers": {
      "description": "Fees paid by the payment system to others for holding funds or executing a transfer",
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "TransferTemplate.json" }
    }
  },
  "additionalProperties": false
}
