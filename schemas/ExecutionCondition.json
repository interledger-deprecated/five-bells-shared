{
  "$schema": "http://json-schema.org/draft-04/schema",
  "title": "ExecutionCondition",
  "description": "Condition for unlocking held funds",
  "type": "object",
  "allOf": [{
    "$ref": "SignedMessageTemplate.json"
  }],
  "oneOf": [{
    "required": ["message"]
  }, {
    "required": ["message_hash"]
  }],
  "required": ["algorithm", "public_key"]
}
